# Chave secreta para a segurança da sessão do Flask.
# Gere uma chave forte e única para produção. Você pode usar o comando: openssl rand -hex 32
SECRET_KEY="sua_chave_secreta_super_forte_aqui"

# Chave da API do AbacatePay para processamento de pagamentos PIX.
ABACATE_API_KEY="sua_chave_da_api_abacatepay_aqui"

# URL de conexão com o banco de dados PostgreSQL.
# Em produção (ex: Render), esta variável será fornecida pelo serviço de banco de dados.
# Para desenvolvimento local, descomente e ajuste a linha abaixo.
# DATABASE_URL="postgresql://USUARIO:SENHA@localhost:5432/NOME_DO_BANCO"

# (Opcional) Para rodar o Flask em modo de depuração localmente.
# Não defina como 'true' em produção.
# FLASK_DEBUG="true"
